# SETUP.md — การตั้งค่าข้อมูลพิกัด

ตอนนี้แอป **มีข้อมูลตั้งต้นมาให้แล้ว** ดึงมาจาก OpenStreetMap ด้วย `tools/osm_import.py`
ติดตั้งแล้วเปิดใช้ได้เลย ไม่ต้องกรอกอะไรก่อน

แต่ข้อมูล OSM เป็นแค่ **จุดตั้งต้น** ไม่ใช่ผลสำรวจ — จุด POI คือ *centroid ของรูปอาคาร*
ที่ลากจากภาพถ่าย และรั้วมอใน OSM ก็ติด tag ว่า `note=Outline completly estimated.`
คลาดเคลื่อนได้หลายเมตร ถ้าอยากให้แม่นจริงต้องเดินเก็บพิกัดเองตามข้อ 2–3

| ขั้น | ทำอะไร | ไฟล์ที่เปลี่ยน | จำเป็นแค่ไหน |
|---|---|---|---|
| 1 | ดึง bbox + POI + เส้นทาง จาก OSM | `campus_config.json`, `pois.geojson`, `paths.geojson` | **ทำมาให้แล้ว** รันซ้ำเมื่ออยากอัปเดต |
| 2 | สำรวจพิกัดจุดต่าง ๆ ด้วย GPS | `app/src/main/assets/data/pois.geojson` | ทำเมื่ออยากให้หมุดแม่นระดับเมตร |
| 3 | บันทึกเส้นทางที่ OSM ไม่มี | `app/src/main/assets/data/paths.geojson` | ทำเมื่อมีทางลัด/ทางเดินที่ OSM ไม่ได้วาด |
| 4 | (ทางเลือก) สร้าง MBTiles สำหรับโหมด full-offline | `app/src/main/assets/map/` | ไม่บังคับ |

---

## 1. ดึงข้อมูลจาก OpenStreetMap

```bash
python3 tools/osm_import.py           # เขียนทับไฟล์ทั้ง 3 ตัว
python3 tools/osm_import.py --dry-run # ดูผลก่อน ไม่เขียนไฟล์
python3 tools/osm_import.py --refresh # ไม่ใช้ cache ดึงใหม่จาก Overpass
python3 tools/geojson_validate.py     # ตรวจก่อน commit ทุกครั้ง
```

สคริปต์จะ:
1. ดึงรูปรั้ววิทยาเขต — OSM `way/518190351` (`amenity=university`, addr 129 / เนินหอม / 25230)
2. ดึง **ถนนและทางเดินทุกเส้น** ในรั้ว + เผื่อขอบ 200 ม. แล้วตัดเฉพาะส่วนที่อยู่ในเขต
3. ต่อปลายทางที่ OSM วาดขาดไว้ห่างกันไม่เกิน 12 ม. เข้าด้วยกัน และตัดเส้นที่เดินไปไม่ถึงทิ้ง
   → กราฟ routing เหลือชิ้นเดียวต่อกันหมด ไม่งั้น A* จะขึ้นว่า "ไม่พบเส้นทาง"
4. ดึงอาคาร/สถานที่ **ที่มีชื่อ** มาเป็น POI แล้วจัดหมวดตาม tag
5. คำนวณ `bbox` = รั้ว + 200 ม. แล้วขยายให้ครอบทุกพิกัดที่เขียนออกมา

### ค่าที่ได้ตอนนี้
```json
"bbox":   { "minLon": 101.338353, "minLat": 14.151579, "maxLon": 101.367897, "maxLat": 14.170261 },
"center": { "lon": 101.352962, "lat": 14.161024 }
```
พื้นที่ **3.19 x 2.05 กม. = 6.5 ตร.กม.** ประมาณ 462 vector tile ที่ z13–18

### ทำเองด้วยมือ (ถ้าไม่อยากใช้สคริปต์)
1. เปิด <https://www.openstreetmap.org/> ค้นหาวิทยาเขต หรือเลื่อนไป **ต.เนินหอม อ.เมืองปราจีนบุรี**
   (ที่อยู่ทางการ: 129 ม.21 ต.เนินหอม อ.เมือง จ.ปราจีนบุรี 25230 — <https://www.kmutnb.ac.th/contact-us.aspx>)
2. กดปุ่ม **Export** → แถบซ้ายจะโชว์ค่า 4 ตัวเรียง `minLon, minLat, maxLon, maxLat` (**lon มาก่อน lat**)

### ข้อควรระวัง
- **เผื่อขอบให้น้อยที่สุดเท่าที่พอ** ค่าเริ่มต้น 200 ม. คือ "แค่พื้นที่มอ" ตามที่ตั้งใจ
  ปรับได้ด้วย `--margin 300` แต่พื้นที่ยิ่งกว้าง แผนที่ออฟไลน์ยิ่งใหญ่และดาวน์โหลดนาน
- **ห้ามตั้ง `maxZoom` เกิน 18** — z19 ขึ้นไปทำให้ไฟล์ใหญ่ขึ้นเท่าตัวโดยไม่ได้รายละเอียดเพิ่ม
  เพราะ vector tile หยุดเพิ่มรายละเอียดที่ z14–15 แล้ว ที่เหลือคือการขยายภาพเวกเตอร์
- ถ้าลบไฟล์ทั้ง 3 ทิ้งเพื่อสำรวจใหม่ตั้งแต่ต้น แอปจะยังเปิดได้แต่ขึ้น error ภาษาไทยบอกว่าต้องตั้งค่าอะไร

---

## 2. สำรวจพิกัดจุดต่าง ๆ (Surveyor Mode)

> **ห้ามก็อปพิกัดจาก Google Maps มาแปะ** เหตุผลอยู่ใน `docs/ACCURACY.md`
> สรุปสั้น ๆ: ภาพดาวเทียมของ Google มี offset กับข้อมูล OSM ได้ 3–15 ม.
> พอเอาพิกัดจาก Google มาวางบน basemap ที่ทำจาก OSM หมุดจะเลื่อนทันที
>
> ข้อมูลจาก OSM ไม่มีปัญหานี้ เพราะเป็นชุดข้อมูลเดียวกับที่ basemap วาดมา
> แต่ก็ยังแม่นสู้การเดินไปยืนวัดเองไม่ได้

จุดที่ควรสำรวจก่อนคือจุดที่ `pois.geojson` ระบุ `needsSurvey: true` (ตอนนี้คือทุกจุด)
และ **ประตูมอ** ซึ่ง OSM แทบไม่มีข้อมูล — ในเขตมอตอนนี้มี `barrier=gate` แค่จุดเดียวและไม่มีชื่อ

วิธีที่ถูกต้องคือเก็บพิกัดด้วย GPS ตัวเดียวกับที่จะใช้นำทาง:

1. ติดตั้งแอป → เข้า **ตั้งค่า → โหมดสำรวจพิกัด (สำหรับผู้ดูแล)**
2. เดินไปยืนที่จุดจริง เช่น ตรงกลางประตูหน้ามอ 1
   - ยืนกลางแจ้ง ห่างตึกและต้นไม้ใหญ่
   - รอให้ค่า accuracy ลงมาต่ำกว่า 10 ม. ก่อนกดเริ่ม
3. กด **"เริ่มเก็บพิกัด"** → แอปเก็บ fix ต่อเนื่อง 30 ครั้ง (~30 วินาที) รับเฉพาะ `accuracy < 15 m`
4. แอปคำนวณผลด้วย **median ของ lat และ lon แยกกัน** (ทนต่อ outlier กว่าค่าเฉลี่ย)
5. กรอก ชื่อ / หมวดหมู่ / คำอธิบาย → บันทึก
6. ทำซ้ำให้ครบทุกจุด (หน้ามอ 1–5, หอชาย, หอหญิง, อาคารเรียน, โรงอาหาร ฯลฯ)
7. กด **Export** → ได้ไฟล์ `pois_surveyed.geojson` ใน `Documents/`
8. ก็อปไฟล์นั้นมาทับ `app/src/main/assets/data/pois.geojson` แล้ว rebuild

**เวลาที่ควรไปสำรวจ:** กลางวัน ท้องฟ้าโปร่ง ไม่ควรทำตอนฝนตกหรือใต้ชายคา

---

## 3. บันทึกเส้นทางเพิ่ม (Track Recording)

`paths.geojson` ที่มาจาก OSM คือ **โครงข่ายเส้นทางจริงในมอ: ถนน + ทางเดิน**
(ตอนนี้ 151 เส้น รวม 29.3 กม. — ถนน 95 เส้น ทางเดิน 51 เส้น + เส้นเชื่อมที่สร้างอัตโนมัติ)
นำทางจากประตูมอไปหน้าอาคารคณะได้เลย เพราะเส้นทางส่วนใหญ่ในมอคือถนน ไม่ใช่ทางเท้า

สิ่งที่ OSM **ไม่มี** และต้องเดินเก็บเอง:
- ทางลัด ทางเดินเลียบอาคาร ทางเดินข้ามสนาม ที่ไม่มีใครวาดลง OSM
- ทางเดินมีหลังคา / บันได ที่อยากให้ router คิดน้ำหนักต่างจากถนน

> **ไม่ต้องเก็บทางเดินภายในอาคาร** GPS ใช้ในร่มไม่ได้ ต่อให้วาดไว้ก็นำทางตามไม่ได้จริง

แอปจึงมีโหมดบันทึกเส้นทางเอง:

1. **ตั้งค่า → โหมดสำรวจพิกัด → บันทึกเส้นทาง**
2. กด **เริ่มบันทึก** → เดินตามทางเดินจริงด้วยความเร็วปกติ
3. แอปเก็บจุดทุก ๆ 3 เมตร
4. กด **หยุด** → แอป simplify ด้วย Douglas-Peucker (epsilon 2 ม.) ให้เส้นเรียบและไฟล์เล็กลง
5. ตั้งชื่อเส้นทาง + เลือกประเภท (`footway` / `road` / `crossing` / `stairs`)
6. เดินให้ครบทุกเส้นทางที่อยากให้นำทางได้ **รวมทั้งจุดตัดของทางด้วย**
7. Export → `paths_surveyed.geojson`

> ไฟล์ที่ export มาจะมีแต่เส้นที่เพิ่งเดิน **ถ้าเอาไปทับตรง ๆ ถนนจาก OSM จะหายหมด**
> ให้เอา `features` ของไฟล์ที่ export ไปต่อท้าย `features` ใน `paths.geojson` แทน
> แล้วรัน `python3 tools/geojson_validate.py` เช็คว่า id ไม่ซ้ำ

### เคล็ดลับให้กราฟเส้นทางต่อกันติด
- เดินให้ **เส้นทางตัดกันจริง ๆ** ที่สี่แยก แอปจะ merge node ที่ห่างกัน < 1.5 ม. ให้เอง
- ถ้าเส้นทางสองเส้นไม่ต่อกัน A* จะหาเส้นทางไม่เจอ → แอปจะขึ้นว่า *"ไม่พบเส้นทางเดินไปยังจุดนี้"*
- ตรวจก่อน commit ด้วย `python3 tools/geojson_validate.py` ซึ่งจะเตือนถ้ามี POI ไหนห่างทางเดินเกิน 30 ม.

---

## 4. (ทางเลือก) สร้าง MBTiles สำหรับโหมด full-offline

โหมดปกติ (`TileSourceMode.OFFLINE_PACK`) ใช้ OpenFreeMap ดาวน์โหลดครั้งแรกก็พอ
แต่ถ้าอยากให้แอป **ไม่ต้องต่อเน็ตเลยแม้แต่ครั้งแรก** ให้ฝัง MBTiles ไปกับ APK

ต้องมีเครื่องมือพวกนี้ก่อน (บน Linux/macOS หรือ WSL):
```bash
sudo apt install osmium-tool wget    # ตัด bbox
# tilemaker: https://tilemaker.org/  (หรือ planetiler ซึ่งเร็วกว่า)
```

แล้วรัน:
```bash
bash tools/build_tiles.sh
```
สคริปต์จะ:
1. ดาวน์โหลด OSM extract ประเทศไทยจาก Geofabrik (~700 MB, ทำครั้งเดียว)
2. `osmium extract` ตัดเฉพาะ bbox ของวิทยาเขต
3. `tilemaker` แปลงเป็น `app/src/main/assets/map/kmutnb.mbtiles` (OpenMapTiles schema)
4. เตือนให้ไปดาวน์โหลด glyph ฟอนต์ที่มีอักษรไทยจาก <https://github.com/openmaptiles/fonts>
   มาวางที่ `app/src/main/assets/map/fonts/`

สคริปต์อ่าน `BBOX` จาก `campus_config.json` ให้เอง ไม่ต้องแก้อะไร
(จะ override ก็ได้ด้วย `BBOX=minLon,minLat,maxLon,maxLat bash tools/build_tiles.sh`)

เสร็จแล้วเปิดใช้ที่ **ตั้งค่า → แหล่งแผนที่ → ใช้แผนที่ที่ฝังมากับแอป**

---

## 5. build และติดตั้ง

```bash
# Windows
gradlew.bat assembleDebug
gradlew.bat installDebug

# Linux / macOS
./gradlew assembleDebug
./gradlew installDebug
```

รัน unit test:
```bash
./gradlew test
```

ตรวจไฟล์พิกัดก่อน commit ทุกครั้ง:
```bash
python3 tools/geojson_validate.py
```
