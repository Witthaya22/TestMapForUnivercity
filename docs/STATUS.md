# STATUS.md — ตอนนี้แอปอยู่ตรงไหน ทำอะไรไปแล้วบ้าง

> **ไฟล์นี้มีไว้อ่านตอนเปิด session ใหม่** ถ้าเปิด Claude ขึ้นมาใหม่แล้วไม่รู้ว่าเกิดอะไรขึ้นมาก่อน
> อ่านไฟล์นี้ไฟล์เดียวก่อน แล้วค่อยไปอ่าน `CLAUDE.md` และ `docs/` ตัวอื่น
>
> อัปเดตล่าสุด: **23 กันยายน 2026**

---

## 1. สรุปใน 30 วินาที

แอปนำทางเดินใน มจพ. ปราจีนบุรี ทำงานออฟไลน์ 100% **ใช้งานได้จริงบนเครื่องจริงแล้ว**
ทดสอบภาคสนามล่าสุดได้ความแม่นยำ GPS **± 1.5 ม.** ซึ่งดีมาก

ช่วงที่ผ่านมาเพิ่ม 2 ระบบใหญ่ และแก้บั๊กจากการใช้งานจริงอีกชุดหนึ่ง:

1. **เสียงเตือนจุดอันตราย** — มีเสียงนำก่อนพูด นำเข้า mp3 เองได้ ลองฟังได้ทุกเสียง
2. **เดินเก็บเส้นทาง** — เดินแล้วเส้นที่เดินกลายเป็นเส้นทางนำทางได้จริง ดูย้อนหลังได้ ส่งออกได้

---

## 2. อยู่ branch ไหน

| | |
|---|---|
| Branch ที่ทำงานอยู่ | `feat/hazard-sounds-and-path-logging` |
| Branch หลัก | `master` |
| รวมเข้า master แล้วหรือยัง | **รวมแล้ว** — merge commit `c8e27cb` (ใช้ `--no-ff` เก็บประวัติครบ ไม่ได้ลบ branch) |
| remote | `origin` = https://github.com/Witthaya22/TestMapForUnivercity.git |
| push ขึ้น GitHub แล้วหรือยัง | **ยัง** — `master` นำหน้า `origin/master` อยู่ **21 commit** |

**ยังไม่ได้ push ให้** เพราะ push คือการส่งของออกไปข้างนอก ต้องให้เจ้าของตัดสินใจเอง
อยากขึ้น GitHub เมื่อไรสั่ง `git push origin master` ได้เลย (หรือบอกให้ทำให้)

**branch เดิมยังอยู่ ไม่ได้ลบ** ถ้าอยากย้อนกลับไปดูว่าฟีเจอร์ชุดนี้ทำอะไรบ้างทีละก้าว
`git log feat/hazard-sounds-and-path-logging` ได้เลย

---

## 3. ทำอะไรไปบ้าง (14 commit)

### ชุดที่ 1 — เสียงเตือนจุดอันตราย

| commit | ทำอะไร |
|---|---|
| `36117e9` | เสียงนำก่อนพูด + นำเข้าไฟล์เสียงเองได้ + 2 เสียงที่ gen มากับแอป |
| `e7175ce` | test กติกาว่าจุดไหนใช้เสียงอะไร |
| `d179981` | เอกสาร `docs/HAZARDS.md` §6 |
| `8806b7c` | วาง mp3 ใน `assets/sounds/` แล้ว build = เพิ่มเสียงให้ทุกคน |
| `797d245` | test เสียงที่ฝังมากับแอป |
| `3a508be` | เอกสารว่าเพิ่มเสียง 2 ทางต่างกันยังไง |
| `ee10eca` | **ย้ายปุ่มนำเข้าเสียงออกมาให้หาเจอ** (เดิมซ่อนอยู่ก้นแผ่นปักจุด) |
| `79159e7` | **เสียงพูดสั้นลง พูดซ้ำ 3 ครั้ง** + ลองฟังได้ทุกคำ + นำเข้าได้ 10 วิ |

### ชุดที่ 2 — เดินเก็บเส้นทาง

| commit | ทำอะไร |
|---|---|
| `42ddb7b` | ระบบเดินเก็บเส้นทาง 2 หน้าจอ (มีแผนที่ / ตัวเลขล้วน) แทนแท็บเดิมในโหมดสำรวจ |
| `0c69f88` | test ตัวบันทึกเส้นทาง + รูปแบบไฟล์ที่ส่งออก |
| `98f90e0` | เอกสาร `docs/PATH_LOGGING.md` |

### ชุดที่ 3 — แก้บั๊กจากการใช้งานจริง

| commit | ทำอะไร |
|---|---|
| `1cf5d78` | ปุ่มลอยทับการ์ดจนกดไม่ได้ — ทุกหน้าที่มีแผนที่ |
| `9662b36` | **หน้าโหมดตัวเลขเด้งออกจากแอป** + **เส้นที่เก็บไม่ขึ้นบนแผนที่** + เพิ่มรูปร่างเส้นทาง + โชว์ค่า GPS บนแผนที่หลัก |
| `881d6be` | จุดหัว-ท้ายของรูปร่างเส้นทางแยกสีไม่ออก |

### ชุดที่ 4 — ส่งออกเฉพาะที่ต้องการ

| commit | ทำอะไร |
|---|---|
| `000d530` | **เลือกติ๊กทีละเส้นว่าจะส่งออกอันไหน** (เดิมมีแต่ตัวกรอง "ทั้งหมด/จากโหมดไหน" ที่อ่านแล้วเหมือนจะส่งออกหมด) + ปุ่มส่งออกในหน้ารายละเอียดของเส้นนั้นเลย |
| `661a219` | test ชื่อไฟล์ตอนส่งออกเส้นเดียว |

---

## 4. ตอนนี้แอปทำอะไรได้บ้าง

### หน้าแผนที่หลัก
- นำทางเดินตามถนน/ทางเดินจริง ออฟไลน์
- **บอกค่าความแม่นยำ GPS ตลอดเวลา** (`± x.x ม.` + แม่นยำดี/พอใช้/ยังไม่แม่น)
- เตือนจุดอันตรายด้วยแบนเนอร์ + เสียง + เสียงพูด

### 4 ระบบเก็บพิกัด (ตั้งใจแยกกัน อย่ารวม)

| ระบบ | ได้อะไร | เข้ากราฟนำทางไหม | เอกสาร |
|---|---|---|---|
| โหมดสำรวจพิกัด (`ui/survey/`) | จุดสถานที่ (POI) | เป็นจุดหมายได้ | — |
| เก็บพิกัด GPS (`ui/gpslog/`) | ค่าที่วัดได้ล้วน ๆ | **ไม่** | `GPS_LOGGING.md` |
| **เดินเก็บเส้นทาง (`ui/pathlog/`)** | เส้นทาง | **ได้ ถ้าเปิดสวิตช์ทีละเส้น** | `PATH_LOGGING.md` |
| จุดอันตราย (`ui/hazard/`) | จุด + รัศมี + เสียง | ไม่ | `HAZARDS.md` |

ทั้ง 4 ระบบมี **2 โหมดคู่กัน**: มีแผนที่ (เห็นภาพรวม) / ตัวเลขล้วน (เปิดเร็ว กินแบตน้อย)

### เสียงเตือนจุดอันตราย
- **เสียงนำ** → แล้วค่อย **พูด** (คำสั้นซ้ำ 3 ครั้ง เช่น `เขตถนนอันตราย, เขตถนนอันตราย, เขตถนนอันตราย`)
- เพิ่มเสียงเองได้ 2 ทาง: นำเข้าในแอป (เฉพาะเครื่องตัวเอง) / วางใน `assets/sounds/` (ติดไปกับแอปทุกเครื่อง)
- ลองฟังได้ทั้งเสียงและคำพูด ที่ **ตั้งค่า → การเตือนจุดอันตราย → เสียงเตือน**

---

## 5. ยังไม่ได้ทำ / รู้อยู่ว่าพัง

| เรื่อง | สถานะ | ขนาดงาน |
|---|---|---|
| **ปิดจอแล้วไม่เตือน** | ยังไม่ทำ — ไม่มี foreground service เลย สิทธิ์ขอไว้ในมานิเฟสต์แต่ไม่มีโค้ดใช้ | ใหญ่ |
| **สวิตช์ "เปิดหน้าจอค้างไว้" กดแล้วไม่มีผล** | **เป็นบั๊ก** ค่าถูกบันทึกลง DataStore จริง แต่ไม่มีที่ไหนเอาไปเซ็ต `FLAG_KEEP_SCREEN_ON` | เล็ก (~3 บรรทัด) |
| A* เลี่ยงจุดอันตรายอัตโนมัติ | ยังไม่ทำ โดยตั้งใจ (เลี่ยงแล้วไม่บอกเหตุผล ผู้ใช้จะงง) | กลาง |
| `PathType` ของเส้นที่เดินเก็บ | ตัดทิ้งตอนเปลี่ยนมาใช้ `track_log` — เส้นที่เดินเก็บนับเป็น footway หมด | กลาง |
| push ขึ้น GitHub | `master` นำหน้า `origin/master` 21 commit — ยังไม่ push | — |

---

## 6. ถ้าพังต้องทำยังไง

### แอปรันไม่ได้ / Android Studio ฟ้อง Activity ไม่มีอยู่

```
Activity class {com.example.testmapforunivercity/...MainActivity} does not exist
```

**ไม่ใช่บั๊กของโค้ด** — เป็น cache ค้างของ Android Studio จากตอนสร้างโปรเจกต์จากเทมเพลต

1. ปิด Android Studio ให้สนิท
2. `rm .idea/workspace.xml`
3. เปิดใหม่ รอ Gradle sync แล้วกด Run

หรือข้าม IDE ไปเลย: `./gradlew.bat installDebug`

### build พังที่ `compileDebugJavaWithJavac` เรื่อง `jlink`

Gradle ไปหยิบ JRE ของ VS Code ที่ไม่มี `jlink` มาใช้ **ต้องตั้ง JAVA_HOME ทุกครั้งก่อน build**

```powershell
$env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"
./gradlew.bat assembleDebug
```

ถ้ายังพัง daemon เก่าอาจค้างอยู่ → `./gradlew.bat --stop` แล้วสั่งใหม่

### อยากย้อนกลับไปก่อนหน้านี้

```bash
git log --oneline                  # หา commit ที่ต้องการ
git revert <commit>                # ย้อนทีละ commit แบบไม่ทำลายประวัติ
```

commit แยกเป็นส่วน ๆ (ฟีเจอร์ / test / เอกสาร) จึง revert เฉพาะส่วนที่พังได้ โดยไม่กระทบส่วนอื่น

### ฐานข้อมูลพัง / อยากเริ่มใหม่

ถอนแอปแล้วติดตั้งใหม่ (`adb uninstall th.ac.kmutnb.prachin.map`) — **แต่ข้อมูลที่เก็บไว้จะหายหมด
ส่งออกไฟล์เก็บไว้ก่อนเสมอ** Room เป็น version 7 และมี migration จริงทุกขั้น ห้ามใช้ destructive migration

---

## 7. คำสั่งที่ใช้บ่อย

```powershell
$env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"   # ต้องทำก่อนเสมอ

./gradlew.bat assembleDebug      # build
./gradlew.bat test               # unit test (ตอนนี้ 179 เคส เขียวหมด)
./gradlew.bat installDebug       # ติดตั้งลงเครื่องที่ต่ออยู่
python tools/make_alert_sounds.py   # gen เสียงเตือนใหม่
```

---

## 8. เอกสารทั้งหมด

| ไฟล์ | เรื่อง |
|---|---|
| `CLAUDE.md` | กฎของโปรเจกต์ · stack · ไฟล์สำคัญ · **วิธีทำงานร่วมกัน** |
| `docs/STATUS.md` | ← ไฟล์นี้ · สถานะล่าสุด · branch · อะไรพังอยู่ |
| `docs/ACCURACY.md` | ความแม่นยำ GPS · ทำไมห้ามก๊อปพิกัดจาก Google Maps |
| `docs/DATA_FORMAT.md` | รูปแบบไฟล์ GeoJSON และ config |
| `docs/OFFLINE.md` | กลไกออฟไลน์ · จอขาว |
| `docs/GPS_LOGGING.md` | เก็บพิกัด GPS (จุด) |
| `docs/PATH_LOGGING.md` | เดินเก็บเส้นทาง (เส้น) |
| `docs/HAZARDS.md` | จุดอันตราย · เสียงเตือน · เสียงพูด |
| `app/src/main/assets/sounds/README.md` | วิธีเพิ่มเสียง mp3 เข้าไปในตัวแอป |
